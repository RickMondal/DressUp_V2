# Prisma
# For local dev tooling (host), connect via host-mapped port (5433)
DATABASE_URL=postgres://dressup:dressup@localhost:5433/dressup

# App
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Auth (must match web)
NEXTAUTH_SECRET=change-me
